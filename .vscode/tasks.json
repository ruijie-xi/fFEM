{
    "version": "2.0.0",
    "tasks": [

        {
            "label": "compile",
            "type": "shell",
            "command": "make -C apps/magnetic test_moving_exact",
            "options": {
                "cwd": "${fileWorkspaceFolder}"
            }
        }
    ]

}
